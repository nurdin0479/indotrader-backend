version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-indotrader}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-indotrader123}
      POSTGRES_DB: ${POSTGRES_DB:-indotrader_db}
    volumes:
      - pg/var/lib/postgresql/data
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-indotrader}"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - backend

  redis:
    image: redis:alpine
    restart: always
    networks:
      - backend
    volumes:
      - redis_/data

  web:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      PYTHONPATH: /app
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    restart: always
    networks:
      - backend
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  worker:
    build: .
    command: python app/worker.py
    env_file:
      - .env
    environment:
      PYTHONPATH: /app
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    restart: always
    networks:
      - backend

  # 🔹 Frontend (React SPA)
  frontend:
    image: nginx:alpine
    volumes:
      - ../indotrader-frontend/dist:/usr/share/nginx/html:ro
      - ./nginx-frontend.conf:/etc/nginx/conf.d/default.conf:ro
    restart: always
    networks:
      - backend    

  caddy:
    image: caddy:latest
    container_name: indotrader-caddy
    restart: always
    dns:
      - 8.8.8.8
      - 1.1.1.1
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - web
      - frontend
    networks:
      - backend

volumes:
  pg
  caddy_data:
  caddy_config:
  redis_data:

networks:
  backend:
    driver: bridge